import { useParams } from 'react-router-dom';

const SignupPage = () => {
  const params = useParams();

  return <main className="flex size-full items-center justify-center">SignupPage</main>;
};

export default SignupPage;
